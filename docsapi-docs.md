# مستندات API اپلیکیشن پیدا (Peyda)

## احراز هویت
تمام درخواست‌ها به جز `/auth/register` نیاز به هدر `Authorization: Bearer <token>` دارند.

## اندپوینت‌های اصلی

### ۱. دریافت دسته‌بندی‌ها
- **متد:** `GET`
- **مسیر:** `/api/v1/categories`
- **توضیح:** لیست تمام دسته‌بندی‌های فعال + وضعیت نیاز به تایید حرفه‌ای

### ۲. دریافت فیلدهای داینامیک
- **متد:** `GET`
- **مسیر:** `/api/v1/categories/{slug}/fields`
- **توضیح:** فیلدهای اختصاصی هر دسته (مثلاً برند و گارانتی برای موبایل)

### ۳. ثبت آگهی جدید
- **متد:** `POST`
- **مسیر:** `/api/v1/ads`
- **بدنه درخواست:**
```json
{
  "title": "تعمیر تخصصی آیفون 13",
  "category_id": 2,
  "price": 500000,
  "attributes": {
    "brand": "اپل",
    "warranty": true
  }
}
```

### ۴. پروفایل اعتماد فروشنده
- **متد:** `GET`
- **مسیر:** `/api/v1/users/{id}/trust-profile`
- **توضیح:** امتیاز اعتماد، تعداد معاملات موفق، وضعیت جواز کسب

## نکات امنیتی
- شماره تلفن فروشندگان فقط پس از شروع چت و تایید هویت خریدار نمایش داده می‌شود.
- آگهی‌های فروشندگان حرفه‌ای قبل از انتشار در حالت `pending_review` قرار می‌گیرند.
